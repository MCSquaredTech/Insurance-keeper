
const ItemsList = ({data, setData, policyHolder, OnSave, OnEdit, onDelete}) => {
  const { id, first, last, phone, email, type } = policyHolder; 
  
  return (
    <>
      
    </>
  )
}

export default ItemsList;
